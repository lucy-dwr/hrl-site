{
  "name": "hrl-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build && cp staticwebapp.config.json dist/staticwebapp.config.json",
    "typecheck": "tsc -b",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "vite": "^5.4.10"
  }
}
